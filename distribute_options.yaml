output: dist/
releases:
  - name: dev
    jobs:
      - name: release-android
        package:
          platform: android
          target: apk
          build_args:
            target-platform: android-arm64
            split-per-abi: true
      # - name: release-ios
      #   package:
      #     platform: ios
      #     target: ipa
      - name: release-macos
        package:
          platform: macos
          target: dmg
      - name: release-windows
        package:
          platform: windows
          target: zip
