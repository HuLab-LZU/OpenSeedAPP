output: dist/
releases:
  - name: dev
    jobs:
      - name: release-android
        package:
          platform: android
          target: apk
          build_args:
            target-platform: android-arm,android-arm64
      # - name: release-ios
      #   package:
      #     platform: ios
      #     target: ipa
      - name: release-macos
        package:
          platform: macos
          target: dmg
      # - name: release-windows
      #   package:
      #     platform: windows
